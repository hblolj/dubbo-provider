<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="provider" owner="hblolj"/>

    <!-- 注册中心地址协议，支持dubbo, multicast, zookeeper, redis, consul(2.7.1), sofa(2.7.2), etcd(2.7.2), nacos(2.7.2)等协议 -->
    <dubbo:registry protocol="zookeeper" address="0.0.0.0:2181"/>

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20880"/>

    <dubbo:protocol name="rmi" port="1099"/>

    <dubbo:protocol name="hessian" port="8080"/>

    <!-- 不同服务不同协议 -->
    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="com.hblolj.dubbo.provider.DemoService" ref="demoService1" version="1.0.0" protocol="dubbo"/>

    <!-- 多协议暴露服务 -->
    <!-- 多版本 -->
    <dubbo:service interface="com.hblolj.dubbo.provider.DemoService" ref="demoService2" version="1.0.1" protocol="dubbo,hessian"/>

    <dubbo:service interface="com.hblolj.dubbo.provider.ProtocolService" ref="protocolService" version="1.0.0" protocol="rmi"/>
    <!--<dubbo:annotation package="com.hblolj.dubbo.provider.impl"/>-->

    <bean id="demoService1" class="com.hblolj.dubbo.provider.impl.DemoServiceImpl"/>
    <bean id="demoService2" class="com.hblolj.dubbo.provider.impl.DemoServiceImpl2"/>

    <bean id="protocolService" class="com.hblolj.dubbo.provider.impl.ProtocolServiceImpl"/>
</beans>